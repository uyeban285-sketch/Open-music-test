{
  "name": "@open-music/infra-docker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "up": "docker compose -f docker-compose.dev.yml --env-file .env.dev up -d",
    "down": "docker compose -f docker-compose.dev.yml down",
    "logs": "docker compose -f docker-compose.dev.yml logs -f"
  }
}
